<Page
    x:Class="laboratoire_3.AjoutProjet"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:laboratoire_3"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="Yellow">

        <StackPanel HorizontalAlignment="Center" Margin="20">

            <TextBlock x:Name="titre" Text="Création d'un Projet" FontSize="25" Margin="0,0,0,15"/>


            <!-- Ajout Employer -->
            <StackPanel x:Name="formEmployer" Background="AliceBlue" Margin="80,0,00,0">
                <TextBox x:Name="tbxNum" Header="Num Employer:"/>
                <TextBox x:Name="tbxdeb" Header="Debut de L'Employer:"/>
                <TextBox x:Name="tbxbudget" Header="Budget:"/>
                <TextBlock x:Name="tblbudget" Text="le Budget doit etre entre 10,000$ et 100,000$" Visibility="Collapsed"/>
            </StackPanel>
            <TextBlock x:Name="tblemployer" Text="Choississez un employer"/>
            <ListView>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:Employe">
                        <TextBlock Text="{x:Bind Matricule,Mode=OneWay}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </StackPanel>
        <Button x:Name="btnvalider" Content="Valider" Background="yellow" Click="btnvalider_Click"/>

    </Grid>
</Page>
